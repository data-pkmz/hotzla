# FRONTEND ENVIRONMENT VARIABLES

# Enable mock auth switcher component in header for local development outside network
VITE_ENABLE_MOCK_AUTH=true

# API Base URL (leave empty when using Vite dev server proxy)
VITE_API_BASE_URL=/api
