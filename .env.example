# ROOT MONOREPO ENVIRONMENT VARIABLES
NODE_ENV=development

# Backend Database Configuration
DATABASE_URL=postgresql://hotzla_user:hotzla_password@localhost:5433/hotzla_db?schema=public

# Authentication Mode (mock for local dev outside domain, iwa for production in domain)
AUTH_MODE=mock

# Server Port
PORT=3001
